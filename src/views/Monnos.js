import React from 'react';
import withSection from '../utils/withSection';
import BlackContainer from '../components/BlackContainer';

const Monnos = () => {
  return (
    <BlackContainer>
      <div className="text-align-center-container">
        <p className="text-align-center">
        We won’t tokenise everything or anything. We are about quality, not quantity and trust is the foundation stone upon which we build our strategy, products and business.
        </p>
       
      
      </div>
    </BlackContainer>
  );
};

export default withSection(Monnos);
